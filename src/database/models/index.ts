import { Account } from './account/entities/account.entity';
import { Problem } from './problem/entities/problem.entity';

const entities = [Problem, Account];

export { Problem, Account };
export default entities;
